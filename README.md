# Certificate Generator

Простой Node.js-сервис для генерации сертификатов из шаблона PNG/JPG.  
На выходе — готовые файлы сертификатов (PNG или PDF) с подставленными:  
**ФИО**, **названием курса** и **уникальным ID**.

---

## 🚀 Как запустить

```bash
npm install
npm start
```

Сервер по умолчанию работает на http://localhost:3000

# Как работает

Загрузите шаблон (PNG/JPG) через /templates

Укажите координаты полей в templates/templates.json

{
  "templates": {
    "default.png": {
      "version": "1",
      "hash": "v1",
      "fields": {
        "name": { "x": -75, "y": 575, "w": 1200, "h": 150, "align": "center", "font": "bold 24px Sans" },
        "course": { "x": 200, "y": 350, "w": 1200, "h": 200, "align": "center", "font": "normal 48px Sans" },
        "id": { "x": 100, "y": 100, "w": 600, "h": 40, "align": "left", "font": "normal 20px Monospace" }
      }
    }
  }
}


Отправьте POST-запрос на /generate с данными:

{
  "template": "default.png",
  "name": "Түгенше Пәленше Пәленбайұлы",
  "courses": ["Node.js Basicsавыаовылоаивыолапвыавыраро", "Decentrathon 4.0"],
  "idPrefix": "CERT-"
}
